朱娅楠
zyn-amber 微信号，加微信备注u-2107-深圳-王
关注 amber-web 视频号
1.特点
（1）知识点多，细碎
（2）知识点之间不连贯
（3）忘的快
（4）视频顺序按照实际上课天看，不用管回看视频的标题
2.愿望
想学的好，就多写（20）。想学的快（预习），想知识牢固（复习，笔记里，代码里的内容）

3.html、css、js三者的关系
网页前台分三个层次：内容层，样式层，行为层。

4.单标签和双标签的区别
单标签不能包裹文字和其它标签，双标签可以包裹文字和其它标签

5.内联元素和块级元素
内联元素也叫作行内元素，不自占一行，内容有多大撑多大，没有内容，就什么都没有。
块级元素，默认（没有单独设定宽度的时候）自占父元素的一行
文字的级别和内联元素的级别是一致的

6.br换行
br换行标签，不能做结构布局使用，建议用在文本换行，比如，两个段落

7.元素的全局属性
（1）id元素的唯一标识，不允许重复，不允许多个，不允许数字开头。如直接获取该元素，传输接收数据的元素
（2）class 类属性，重点，代表当前元素归类，不允许数字开头，常用于样式的设置,可以写多个类的值，中间用空格分卡，也可以和其它元素具有相同的类名

8.空连接
<a href="javascript:void(0);">void(0)返回undefined不做任何处理</a>
<a href="javascript:;">执行一条空语句</a>


day02重点
1.工作中ui设计给web图片一定大于你需要设定的大小

2.列表项li不能单独存在,必须包裹在列表标签中.列表标签的第一层必须是列表项标签

3.表格
(1)移动端开发布局不许用表格,表格自己写的样式很丑,最好用ui框架修饰
(2)只要写表头thead必须主体要写tbody用tbody包裹行

4.表单
(1)value="" 初始值,最重要的,使用表单标签的目的是为了手机用户数据,value中存的正式用户数据.value的值必须通过js事件获取

(2)checked被选中的
如果直接写在标签中代表默认被选中.不写值为true,默认就是true,因此checked属性是可以不写值的属性
用户选中一个选框,这时该选框就带有checked属性,js通过获取所有选框找到checked值为true的选项的元素,通过元素value值获取用户选的是哪个选项


day03重点
1.http
http和其它相关知识的关系
工作原理（重）
报文
http 状态码，给前端开发人员看的，通过状态码得知前后台交互的情况

2.ajax
异步交互的概念
XMLHttpRequest对象（xhr）相关内容
创建、设置请求信息、发送、接收响应

3.node
创建服务器，通过服务器的接口接收和返回数据
（知识点薄弱的，赶紧回看视频）


day05
1.用id获取元素的方法
在html页面上的元素，需要设置一个id属性。在js脚本中利用document.getElementById('元素的id值')获取该元素（标签及内部所有内容）

2.点击事件
onclick是属性，可以放在标签中
<button onclick="change()">修改文本内容</button>
在脚本里写一个有名字的函数，通过标签中的onclick属性触发或者说调用函数
另一种方法，与onreadystatechange 写法相似
元素.onclick=function(){} 在js脚本中直接使用元素的点击属性调用一个匿名函数

3.value属性
具有value属性的元素，是可以通过该属性设置或获取数据的，大多为表单元素具有value属性
设置<input type="text" value="tom">
获取 input元素.value ,前提是获取value属性值一定是要有事件触发的

4.innerHTML
代表可以获取元素的“内容”，也可以富裕元素“内容”。
获取：<div id="show">王科123</div>
元素.innerHTML 直接使用就是：王科123
赋予：元素.innerHTML = xxxx

5.json格式
json格式和js中的Object数据类型很像，但是数据类型是要存放在变量中的，json不是他只是一种格式的名称。
json写属性的时候，属性名必须加双引号。所有字符串必须是双引号。
json的最后一组数据，结尾不允许加逗号

day06重点：
1.特别容易错的地方
（1）路径 ./ 当前目录下的
（2）路由 把路由地址写成普通路径地址 ./user，报404
（3）ajax post方法的请求头，单词太多并且没提示
（4）路由没暴露，app就引入
（5）在模块文件中如果创建了路由对象，就是用（router）路由对象的名字，不是使用app
（6）接收时，先看xhr.responseText 返回数据，再变成对象，确定有数据再转换
（7）创建文件时不注意将文件放错文件夹，报错404


day09重点

1.元素的宽度和高度
（1）一种是通过css设置元素的宽度和高度（块级元素）
（2）一种是通过内容撑开的宽度和高度（内联元素）
（3）一种情况，块级元素没设置宽度高度，但有内容，宽度为父级元素的宽度，高度为内容撑开的高度（注意高度如果是撑开的，无法给子元素当作%的参照）

2.边框
边框在布局中占据了重要地位，因为它可以以多种形式呈现效果，还会影响布局

3.幽灵空白节点
行内块显示元素特有，如果需要去掉可以使用以下两种方式
（1）把img元素变成块级元素 display:block,这个方法比较好，并且不会影响其他元素
（2）父元素，将font-size字号统一为0，也可以清除幽灵空白节点，但父元素中有其他带有文字的元素，也会将文字变为0大小，会影响其他元素
