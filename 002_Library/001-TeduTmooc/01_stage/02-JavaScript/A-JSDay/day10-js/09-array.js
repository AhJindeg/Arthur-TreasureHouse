/*

var arr=[];
// 索引数组，以>=0整数作为下标
// 关联数组，以字符串作为下标
arr['id']=1;
arr['name']='tao';
arr.sex='男';

console.log(arr);

*/

// 创建数组包含有一组商品，每一个商品是一个元素，包含的属性有标题，价格
var aBac=[
	{title:'华为P50',price:5799},
	{title:'小米11',price:4299}
];

console.log(aBac);